{{ form_start(form, {'attr': {'class': 'form-container'}}, {'action' : path('user_create')}) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success self-end">{{ 'edit' in app.request.get('_route') ? 'Editer' : 'Créer l\'utilisateur' }}</button>
{{ form_end(form) }}